\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{experiments/results/detection_vs_fm.png}
  \caption{Message-local detection against fragment size normalised by the minimum matchable unit $m$, for the ten payload classes. Detection is governed by $f\geq m$, not by a rate: the dashed line is $f=m$, and 9 of 423 conditions deviate, all in \texttt{connection\_string}, whose matcher fires on a prefix. Reconstructed matching detects every condition and is invariant to $f$, so it is not plotted.}
  \label{fig:detection-fm}
\end{figure}
