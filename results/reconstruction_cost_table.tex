\begin{table}[t]
  \caption{Reconstruction cost of a stream auditor against a message-local
  monitor. Buffer and aggregate are exact: retained bytes per stream relative to
  the message-local monitor, and the total at 100 concurrent streams.
  $\Delta$ is the median completion-time difference over 41 repeats and
  IQR its interquartile spread, on non-matching stream content (Windows AMD64, CPython 3.10.11,
  single process). No configuration resolves $\Delta$ above its own spread, so
  the latency column is reported as unresolved rather than as a deployment cost.}
  \label{tab:recon-cost}
  \centering\footnotesize
  \setlength{\tabcolsep}{3pt}
  \begin{tabular}{@{}rrrrrrr@{}}
    \toprule
    $L_i$ & $f$ (B) & Frags & Buffer & Agg.\ (MiB) & $\Delta$ (ms) & IQR (ms) \\
    \midrule
    4 KiB & 256 & 16 & 16$\times$ & 0.4 & +0.02 & 0.07 \\
    64 KiB & 256 & 256 & 256$\times$ & 6.2 & +0.59 & 2.27 \\
    64 KiB & 4096 & 16 & 16$\times$ & 6.2 & -0.21 & 1.33 \\
    1024 KiB & 4096 & 256 & 256$\times$ & 100.0 & +3.49 & 19.92 \\
    \bottomrule
  \end{tabular}
\end{table}
