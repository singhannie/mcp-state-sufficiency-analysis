\begin{table*}[t]
  \caption{Design-space registry for MCP 2026-07-28 surfaces. The generated
  artifact also includes a self-scored documentation worksheet, but this table
  is not an empirical result.}
  \label{tab:predictions}
  \centering\footnotesize
  \begin{tabular}{@{}p{0.18\textwidth}p{0.30\textwidth}p{0.10\textwidth}p{0.34\textwidth}@{}}
    \toprule
    Surface & Protocol/host visibility & Unit-aligned? & Registered prediction \\
    \midrule
    server/discover & client/runtime; model-facing if host exposes catalog & yes & capability visibility and cached provenance are host-managed \\
    tools/call & client/runtime; often model-facing through tool planning & yes & single call visible; compositions need history \\
    notifications/progress & client/runtime or UI; model-facing only if host forwards progress text & no & textual progress-message content requires reconstruction when policy-relevant \\
    MRTR input\_required & client/runtime and user-input UI; model context is host-dependent & yes & approval visible; task continuity needs protocol state \\
    response \_meta & client/runtime; model-facing only if host forwards metadata & yes & depends on whether metadata reaches policy gate \\
    resource/task event streams & client/runtime; normally not model-facing unless host summarizes events & type-dependent & continuity/provenance risk; not automatically content fragmentation \\
    Tasks & client/runtime/task monitor; model-facing summaries are host-dependent & partial & history/context policy required for task updates \\
    Mcp-Method / Mcp-Name & HTTP gateway/runtime; normally not model-facing & yes & gateway-visible, not automatically model-facing \\
    cacheable list results & client/runtime cache; model-facing through host-selected list content & yes & conformance risk if reuse decision omits parameters or authorization partition \\
    application-state handles & host-dependent; visible when handles are passed in tool context & yes & visible token; authorization needs provenance binding \\
    \bottomrule
  \end{tabular}
\end{table*}
