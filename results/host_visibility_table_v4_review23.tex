\begin{table*}[t]
  \caption{Host-visibility sensitivity for MCP 2026-07-28 surfaces. The
  table distinguishes protocol/runtime visibility from what a host chooses
  to place in the model context.}
  \label{tab:visibility}
  \centering\footnotesize
  \begin{tabular}{@{}p{0.18\textwidth}p{0.36\textwidth}p{0.36\textwidth}@{}}
    \toprule
    Surface & Protocol/host visibility & Policy-relevant state \\
    \midrule
    server/discover & client/runtime; model-facing if host exposes catalog & authorization-scoped capability catalog \\
    tools/call & client/runtime; often model-facing through tool planning & invocation parameters plus call history \\
    notifications/progress & client/runtime or UI; model-facing only if host forwards progress text & complete progress stream plus progress token \\
    MRTR input\_required & client/runtime and user-input UI; model context is host-dependent & pending call, requested input, replayed answer \\
    response \_meta & client/runtime; model-facing only if host forwards metadata & metadata plus tenant/provenance context \\
    resource/task event streams & client/runtime; normally not model-facing unless host summarizes events & event stream id, notification type, resource/task correlation \\
    Tasks & client/runtime/task monitor; model-facing summaries are host-dependent & task id, task state, update history \\
    Mcp-Method / Mcp-Name & HTTP gateway/runtime; normally not model-facing & HTTP headers plus body/provenance \\
    cacheable list results & client/runtime cache; model-facing through host-selected list content & cacheScope, ttlMs, parameters, authorization context \\
    application-state handles & host-dependent; visible when handles are passed in tool context & handle provenance and permitted operations \\
    \bottomrule
  \end{tabular}
\end{table*}
